# 실험 2 리포트 초안: 진행 상태 접지 (상태선 + 파이프 안내)

- 사전등록: `pre-registration.md`(승인 커밋 `6cda60f`, 승인 2026-07-18;
  개정 1 커밋 `4ccecb6` — 스포트 재측정, 사용자 승인)
- 작성: 러너(무인 수행) 초안 + 컨트롤러 Task 10 판정 확정(§7)

## 1. 배치 ↔ 커밋 ↔ 스탬프 (조건 검증 기록, PROTOCOL §5)

| 배치 | 스탬프 | 실행 시 `git rev-parse HEAD` | exit | 소요(하네스) | effective_config (실측) |
|---|---|---|---|---|---|
| 개입 8K uv+fm (3차) | `20260718T082449Z` | `030594031dab29314bd881e712adf7870804b1b4` | 0 | 3672.2s ≈ 61.2분 | ctx 8192 / out 4096 / timeout 240 / temp 0.1 — 사전등록 대조값과 일치 |
| 개입 tasks/ 스포트 | `20260718T092740Z` | `0305940…`(동일) | 0 | 2524.8s ≈ 42.1분 | ctx 8192 / out 4096 / timeout 60 / temp 0.1 — v2 조건 일치 |
| 개입 스포트 재측정 (개정 1) | `20260718T115152Z` | `0305940…`(동일) | 0 | 2350.5s ≈ 39.2분 | ctx 8192 / out 4096 / timeout 60 / temp 0.1 — v2 조건 일치 |
| 개입 32K 검증 | `20260718T101234Z` | `0305940…`(동일) | 0 | 4732.5s ≈ 78.9분 | ctx 32768 / out 4096 / timeout 240 / temp 0.1 — 대조 164905Z와 일치 |
| 대조 8K (재사용) | `20260717T152633Z` | `3f971291…`(M10 리포트 §1 기록) | — | 4308.2s | 상동(8K) |
| 대조 32K (재사용) | `20260717T164905Z` | `3f971291…`(상동) | — | 4805.9s | 상동(32K) |
| 대조 스포트 (재사용) | `20260717T215729Z` | (M10 승자 검증 배치) | — | 2255.9s | 상동(v2) |

- 모든 신규 배치 report.json `model` = `ornith-1.0-9b`, `interrupted` = false.
- lms 로드 검증(배치별 `curl /api/v0/models`): 8K = 12288, 스포트 = 8192,
  32K = 49152 — 각 배치 전 ornith-1.0-9b 단독 loaded 확인(타 모델 not-loaded).
- 배치 전 게이트: `m11/status-note`(=`0305940`) 체크아웃 후 `cargo build`,
  `eval tasks --verify` 12/12, `eval tasks-large --verify` 3/3 통과.
- 트립와이어 `${TMPDIR}/.cargo` 부재 확인(시작 시).
- 측정 종료 후 lms 상태: ornith-1.0-9b 단독 로드, ctx 8192 (개정 1 재측정
  배치 상태 그대로 — `lms ps` 실측).

## 2. 수행 경위 (이상 사항 정직 기록)

8K 본배치가 2회 연속 무보고 사망했다. 원인은 모델·코드·데이터가 아니라
**실행 하네스의 백그라운드 작업 60분 수명 상한**이다(트랜스크립트 mtime으로
확정 — 1차 59분57초, 2차 59분43초 시점에 프로세스 강제 종료, 로그에 LLM
에러 0건, 정상 진행 중이었음).

| 시도 | 스탬프 | 결말 |
|---|---|---|
| 1차 | `20260718T042806Z-discarded-partial` | 59분57초에 killed(uv 8/10 진행 중, 트랜스크립트 18개) — 부분 데이터 폐기 표시 |
| 2차 | `20260718T052921Z-discarded-partial` | 59분43초에 killed(트랜스크립트 19개) — 폐기 표시 |
| 3차 | `20260718T082449Z` | 사용자 승인 하에 python `os.setsid` 데몬화로 하네스 수명 상한 밖에서 분리 실행 — 61.2분 완주, BATCH_EXIT=0 |

- 사전등록 중단 규칙 적용: 1차 사망 → 폐기·재수행, 2차 사망(재실패) → 정지·
  보고. 사용자에게 원인 보고 후 **3차 수행 승인**을 받아 수행 메커니즘만
  교체(조건·표본·시드·판정 규칙 불변)해 재개했다.
- 스포트·32K 배치도 동일한 setsid 데몬화 패턴으로 실행해 재발을 방지했다.
- **시간 예산 초과**: 배치별 상한(8K 90분, 스포트 60분, 32K 90분)은 각 시도
  단위로는 모두 준수했으나(61.2 / 42.1 / 78.9분), 2회 사망분 GPU 시간
  (~120분)이 추가돼 8K+스포트 총합 실소요는 사전등록 총상한 ≈2.5h를
  초과했다(≈ 60+60+61+42 = 223분 ≈ 3.7h). 초과분은 전적으로 하네스 수명
  상한 사고에 기인하며 측정 조건에는 영향이 없다(폐기 배치의 데이터는 일절
  사용하지 않음).
- 사전등록 상태 게이트: 최초 기동 시 문서 상태가 "초안"이어서 즉시 중단·
  보고했고, 사용자 승인 반영 커밋 `6cda60f`(상태 "승인됨") 후 재개했다.
- **개정 1 배치**: 1차 스포트 게이트 미달의 재검토(법의학: 실패 6런 전수
  상태선 비귀속) 결과, 사용자 승인 개정(`4ccecb6`)으로 스포트 1회 재측정을
  사전 공약 규칙과 함께 등록·수행했다(§5). setsid 패턴, 39.2분(상한 1.0h
  준수), 게이트(브랜치 0305940·config v2·lms 8192) 재검증 후 기동.

## 3. 주 지표 판정 (uv 전용, per-run TSV 재계산 — 사전등록 "카운팅 유의점" 적용)

개입군 8K(082449Z)의 `run-update-vat-rate-*` 10행 재계산 결과:

| 지표 | 대조 (152633Z) | 개입 (082449Z) | 사전등록 규칙 | 기계 적용 결과 |
|---|---|---|---|---|
| ① zero_mut max_turns(uv) | 3/10 (런 1·2·7) | **0/10** | < 3 (감소) | **충족** (동률 아님, 0 < 3) |
| ② cargo_after_mut(uv) | 3/5 (뮤테이션 런 3·4·6·8·9 중 3·4·9) | **5/7** (뮤테이션 런 1·3·4·6·7·8·9 중 1·4·6·8·9) | > 3/5 (상승) | **충족** — 분모가 5→7로 달라졌으나 5/7≈0.714는 3/5=0.6 대비 명백한 초과로 판정 트리 2번(경계선: "근접하나 동률도 명백한 초과·미달도 아닌 값") 비적용 |
| 열세 축 zero_mut finished(uv) | 2/10 (런 0·5) | **2/10** (런 0·5) | > 2이면 위반 | **무저촉** (2 = 2, 초과 아님) |

- **소표본 전수 나열**(①·열세 축 관련, 개입군 zero-mut 런 3건 전수):
  - `run-update-vat-rate-0`: finished / passed=False (조기 finish)
  - `run-update-vat-rate-2`: repetition_stop / passed=False
  - `run-update-vat-rate-5`: finished / passed=False (조기 finish)
  - max_turns 종결 zero-mut 런: **0건** — H1 방향으로 소멸. 대조의 finished
    2건(런 0·5)은 그대로이고 max_turns 3건(런 1·2·7)이 뮤테이션 도달 런으로
    전환된 구도다(개입군 뮤테이션 런 5→7). repetition_stop 1건(uv-2)은
    대조에 없던 종결(zero_mut other=1)로 Task 10 정독 대상.
- 주의: 개입군 uv `false_finish_count`=3(uv-0·5·8)이지만 uv-8은
  `first_mut_turn`=10(뮤테이션 있음)이라 열세 축(zero_mut finished)에는
  불산입 — 사전등록이 경고한 "false_finish와 zero_mut finished 불일치"가
  실제로 발생했다.

**주지표 기계 적용 결과: ①·② 모두 충족 ∧ 열세 축 무저촉 → "주지표 우세"**
(이에 따라 사전등록 조건부 32K 검증 배치를 수행했다.)

단, 아래 §5 스포트 게이트 미달이 있어 **승자 규칙의 병합 판정과는 별개로
"병합 보류 재검토 대상"**이다(사전등록 보조 지표 게이트 조항). 최종 판정은
컨트롤러/사용자 몫.

## 4. 보조 지표 (8K)

| 지표 | 대조 (152633Z) | 개입 (082449Z) | 비고 |
|---|---|---|---|
| uv 통과율 | 1/10 | **2/10** (uv-6, uv-9) | 상승 방향 — 지지 강화(승패 조건 아님) |
| fm 통과율 | 10/10 (엄 8) | **8/10** (엄 7, ff 0) | **10/10 미달 → 보조 지표 열위로 기록**(사전등록 fm 비악화 조항). 실패: fm-3 repetition_stop, fm-4 max_turns |
| uv false finish | 2/10 | 3/10 (uv-0·5·8) | 열세 축(zero_mut finished)은 2로 동일 — uv-8은 뮤테이션 후 조기 finish |
| status_note 발동 | 0 (코드 없음) | 60회 (스포트 77, 32K 86) | 트리거 정상 발동 — "정보 0" 사고 없음 |
| pipe_note 발동 | 0 | 9회 (스포트 2, 32K 13) | 상동 |
| status_in_args | 0 | **0** (전 배치 0) | `[status]` 복사 오염 미관측 — 스펙 §9 리스크 미발현 |
| sr recovered | 20/30 | 23/30 | M10 승계 — 비악화(개선) |
| stop_cause sr | 0 | 0 | 섭동 회귀 없음 |
| avg_duration_secs | 211.6s | 180.0s | 시간 계열 — 대조군 재사용 한계 각주에 따라 참고치 |
| 오버플로(400 context) | — | 0건 (전 신규 배치 로그 grep) | |
| salvage | 0 | 0 (전 배치) | |

## 5. tasks/ 스포트 (게이트 지표 — 1차 + 개정 1 재측정 병기)

| | 대조 (215729Z) | 개입 1차 (092740Z) | 개입 재측정 (115152Z, 개정 1) |
|---|---|---|---|
| 통과 | 35/36 (엄 34, ff 0) | **30/36 (엄 28, ff 0)** | **33/36 (엄 31, ff 0)** |
| 게이트 ≥33/36 | 충족 | **미달** | **충족 (33 ≥ 33)** |

**개정 1 규칙 기계 적용**(`4ccecb6` 사전 공약): 재측정 33/36 ≥ 33/36 →
**"1차 30/36은 분산으로 판정, 병합" 방향에 해당**. (<33이면 비병합 확정
조항은 발동하지 않음.) 최종 판정 문안은 컨트롤러 Task 10이 확정한다.

1차 실패 6런 전수(전부 repetition_stop):

| 런 | outcome(1차) | 비고(TSV) | 재측정 결과 |
|---|---|---|---|
| run-edit-crlf-file-2 | repetition_stop | first_mut=2, finish_args_corr=1 | **재실패** (repetition_stop) |
| run-fix-compile-error-0 | repetition_stop | zero-mut (first_mut=0), sr 1 | 통과 (법의학: 주입 前 분기 — 서버측 비결정성) |
| run-fix-failing-test-0 | repetition_stop | zero-mut, sr 2 | 통과 |
| run-multiline-string-edit-0 | repetition_stop | first_mut=5, sr 2 (대조에서도 이 과제 r0 유일 실패 — repetition_stop) | **재실패** (repetition_stop) |
| run-multiline-string-edit-1 | repetition_stop | first_mut=3, sr 2 | **재실패** (repetition_stop) |
| run-rename-function-2 | repetition_stop | first_mut=2, cargo_after_mut=1 | 통과 |

- 재측정 실패 3런은 모두 1차 실패의 부분집합(신규 실패 과제 없음) —
  edit-crlf-file r2, multiline-string-edit r0·r1. multiline-string-edit
  r0는 대조에서도 유일 실패였고, 재측정 TSV상 3런의 원인 프로파일은
  1차와 동일(sr 2·repetition_stop 계열, 개정 1 법의학의 "상태선 무관
  원인"과 합치). 1차 전용 실패 3런(fix-compile-error r0,
  fix-failing-test r0, rename-function r2)의 회복은 분산 판정의 보조
  근거다.
- 1차 관측(대조 대비 repeat_corr 4→10, finish_missing 9→7, stops other
  1→8; 통과한 repetition_stop 2건: count-usages-0, find-definition-1)은
  기록 유지. 재측정에서는 stops other 5(실패 3 + 통과 2:
  count-usages-0, rename-function-0), repeat_corr 10, finish_missing 9.

## 6. 32K 검증 배치 (조건부 — 수행함)

수행 근거: §3 주지표 우세(①·② 충족 ∧ 열세 축 무저촉). 사전등록 §"32K 검증
배치" 조건 성립.

| | 대조 (164905Z) | 개입 (101234Z) |
|---|---|---|
| 전체 | 13/20 (엄 12, ff 0) | **13/20 (엄 12, ff 0)** |
| fm | 10/10 (엄 10) | 9/10 (엄 9) — fm-6 max_turns |
| uv | 3/10 (엄 2) | 4/10 (엄 3) — uv-0(max_turns 통과)·2·3·5 |
| zero_mut (uv) | 0건 | 0건 |
| cargo_after_mut (uv 전용) | 6/10 | 4/10 |
| cargo_after_mut (합산) | 16/20 | 14/20 |
| sr recovered | 36/45 | 22/48 |
| avg_duration_secs | 236.3s | 232.8s (참고치) |

- 총점 동일(13/20), 과제 구성만 이동(fm −1, uv +1). 32K에서는 양군 모두
  zero-mut 런이 없어 ①·② 판정 축 자체가 8K와 다른 지형이다(사전등록상
  32K는 검증 배치이며 별도 판정 임계 없음).
- uv 뮤테이션 런의 cargo 검증률(4/10)과 sr recovered(22/48)는 대조보다
  낮다 — Task 10 정독 대상으로 기록.

## 7. 판정 규칙 적용 결과 요약

- 주지표: ① 충족(0<3), ② 충족(5/7>3/5) → **주지표 우세**
- 열세 축: 무저촉(2=2)
- 보조 지표: uv 통과율 상승(지지), **fm 10/10 미달(열위 기록 — 개정 1
  조항대로 판정 대상 아님, 보조 기록 유지)**, status_in_args 0(리스크
  미발현), 오버플로·salvage 0
- 스포트 게이트: 1차 30/36 미달 → 병합 보류 재검토 발동 → 법의학(상태선
  비귀속) → **개정 1(`4ccecb6`) 재측정 33/36 ≥ 33/36 — 기계 적용 결과:
  1차를 분산으로 판정, 병합 방향** (§5)
- 종합(기계 적용): 주지표 우세 ∧ 열세 축 무저촉 ∧ 개정 1 게이트 충족 →
  승자 규칙 문면상 병합 대상.

**최종 판정 (컨트롤러 Task 10, 2026-07-18 확정): 승 — 병합.** 사전등록
원본과 개정 1의 규칙을 기계 적용한 결과이며 사후 재해석 없음. 병합 커밋
`fceb2bd`(--no-ff), 병합 후 게이트 318+2 tests·clippy 무경고·verify
12/12·3/3 통과. 분산 판정의 보조 근거: 재측정 실패 3런이 1차 실패의 진부분집합
(신규 실패 0), 회복 3런이 법의학의 분산 귀속 런과 정확히 일치, multiline r0는
대조군 유일 실패와 동일 seed·동일 원인. 잔여 관측(비차단): fm@8K 8/10 열위
기록, 32K sr recovered 22/48(대조 36/45) — M12 후보 입력.

## 8. 원자료 위치

- 개입 스탬프: `.loco/eval/20260718T082449Z`(8K), `…T092740Z`(스포트 1차),
  `…T115152Z`(스포트 재측정, 개정 1), `…T101234Z`(32K); 폐기:
  `…T042806Z-discarded-partial`, `…T052921Z-discarded-partial`(분석에
  불사용)
- 대조 스탬프: `.loco/eval/20260717T152633Z`(8K), `…T164905Z`(32K),
  `…T215729Z`(스포트)
- exp_metrics 런별 TSV: 본 초안 §3~§6 수치는 모두
  `python3 scripts/exp_metrics.py <개입 스탬프> <대조 스탬프>` 출력에서
  재계산(8K쌍·32K쌍·스포트쌍)
- 배치 stdout 로그(스크래치, 세션 한정):
  `batch-8k-try3.log`·`batch-spot.log`·`batch-32k.log`·
  `batch-spot-rerun.log`
