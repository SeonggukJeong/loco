# M14 설계 — 정직한 검증 신호 II와 확정 결함 정리

- 상태: **개정 1 — 전문 리뷰 1R(`Ready: No`) Critical 3·Important 5·Minor 4 전건 반영.
  재리뷰 대기**
- 작성일: 2026-07-20
- 기준 커밋: `aa7a34c` (= `origin/main`, M13 완료 시점)
- 선행 문서: `docs/m14-candidates.md`(M13 T12 산출), `docs/baselines.md` "M13",
  `docs/superpowers/specs/2026-07-18-m12-honest-harness-design.md`
- 스코핑 근거: 사용자 결정 4건(2026-07-20 브레인스토밍) — §2-2에 기록
- 리뷰 이력: 1R 초안 `cd093b1` 대상 — Critical 3·Important 5·Minor 4·Nit 4,
  전건 처분(§12). **반박한 항목 0건** — 리뷰어가 뮤테이션·라이브 서버 호출·이항
  계산으로 검증했고, 컨트롤러가 Critical 3건을 독립 재확인했다

## 1. 배경 — 왜 이 축인가

M13은 북극성을 합성 평가에서 실사용으로 옮겼고, 그 첫 관측이 헤드라인을
뒤집었다: **합성 통과율이 실사용을 예측하지 못한다.** 동시에 파일럿은 loco
하네스 자신의 결함을 여러 형태로 드러냈다.

M12는 "테스트가 통과했는가"를 exit code 대신 파싱된 libtest 결과에 접지했다.
파일럿이 보여준 것은 그 위층이 비어 있다는 사실이다 — **"맞는 것을 테스트했는가"는
아무것도 접지하지 않는다.** `docs/m14-candidates.md` §1-3이 다섯 형태를 센다.

| 형태 | 파일럿 관측 | 기전 |
|---|---|---|
| 파이프가 exit 0 날조 | 5건 | `has_unquoted_pipe`가 노트 채널에만 연결 |
| 컴파일 실패인데 검증으로 접수 | 3건 | **해제 술어가 종료 코드를 안 본다** — §3-3-2 |
| `tail`이 실패 섹션 절단 | 1건 | 통과 섹션만 남아 규칙 2·4를 모두 통과 |
| `cargo test --lib`을 bin 전용 크레이트에 | 4건 | 실행 0건 |
| 크레이트 스코프 오지정 | 2건 | `ran==0 ∧ filtered_out==0` — M12 공허 런 가드가 침묵 |

**계수 주의(리뷰 I4)**: 위 숫자는 **다중 라벨 기준 건수(합 15)이지 세션 수가
아니다.** `docs/baselines.md` M13이 같은 이유로 *"다중 라벨이라 합(42) ≠ 세션
수(20)"*를 못박고 있고, 이 스펙 자신의 §3-3도 #2가 #1과 겹친다는 것을 인정한다
(파이프 기인분). **"15세션에서 관측됐다"고 쓰면 안 된다.**

초판은 #2의 기전을 *"`test result:` 줄 부재 → `parse_test_summary`가 `None`"*으로
적었는데 **틀렸다**(리뷰 I5). 컴파일이 실패한 `cargo test`는 exit 101이지 0이
아니다. 실제 기전은 §3-3-2에 적는다.

이 축을 고르는 근거는 세 가지다. ① 검증 날조는 봉투 안/밖과 무관하게 발생한다 —
과제문이 파일 경로를 줘도 모델은 exit 0을 날조한다. ② M12 스코핑에서 사용자가
정한 원칙("잘 못하는 걸 잘하게 만들려고 시간 쓰지 말고, 하네스 자신의 거짓
초록불을 고쳐라")의 직접적 연속이다. ③ 파일럿의 성공 판정 4건이 실행·대조하면
뒤집혔다는 관측(`docs/m14-candidates.md` §4)과 같은 뿌리다.

### 1-1. 이 마일스톤이 무엇을 주장하지 않는가

**효과는 `tasks/`로 실증되지 않는다.** 검증 날조는 파일럿 실레포에서 관측됐지만
합성 세트에서는 거의 발동하지 않는다. M12가 같은 상황을 명시적으로 받아들여
회귀 게이트로 축소했고, M14도 같다. 측정은 **무회귀 확인 + 장치 발동 여부
관측**이며, **효과 확인은 다음 파일럿의 몫이다.**

**게이트의 검출력도 낮다**(리뷰 M2, §8-1). 36런 이항으로 통과율 97.2% → 87%의
회귀는 재측정을 포함해도 **약 절반이 그냥 통과한다.** 따라서 §7의 "무회귀"는
*"통과율 80% 미만의 붕괴는 배제한다"* 이상을 뜻하지 않는다. 게이트 통과로
"검증 날조가 줄었다"를 주장하면 안 되는 것은 물론이고, **"회귀가 없다"조차
그 해상도 안에서만 참이다.**

## 2. 범위

축 넷 + 문서 1건.

- **A. 검증 접지 상위층** (기함) — 파이프 가드를 거부 술어로, 알림 전면, 편집 결과 가시화
- **B. 확정 결함 티켓** — `reasoning_content`, `pack()` 데이터 손실, 집계 2건
- **C. 도구·계측 부채** — `pilot.sh`, 트랜스크립트 영속화, `exp_metrics`
- **D. 봉투 명시** — 코드 변경 0, 문서만

**축 간 상호작용이 이 마일스톤의 주 리스크다**(리뷰 총평). 세 쌍이 서로를
침식하므로 §10에 구현 순서를 못박는다.

### 2-1. 범위 밖 (명시적 제외)

| 제외 항목 | 근거 |
|---|---|
| `pack()` 수선 (a) — `messages[1]` 인덱스 고정 | `docs/m14-candidates.md` §1-1이 착수 금지로 판정. 쌍 삭제 루프에 마지막 메시지 보호가 없어 종료성 설계가 선행돼야 한다(실측 확인, §4-2) |
| 공허 런 거래 반전 (#4·#5의 **거부** 처리) | §3-2 — 알림만 한다 |
| **비-파이프 실패 검증(exit≠0)의 거부** | §3-3-2 — 이것을 막는 것은 §3-2가 기각한 "긍정 증거 요구"에 해당 |
| 탐색형 하네스 (2-2 read_file 푸터 · 2-3 누적 카운터 · 2-4 grep 절단) | 사용자 결정 — 봉투 밖에 하네스를 투자하지 않는다 |
| 3-1 뮤테이션 0회 finish 게이트 | 풍선효과 위험 높음(탐색 조기 종결). 논의 선행 필요 |
| 3-2 Vulkan iGPU 오프로드 | 성능 항목, 파일럿 실패 범주와 무관. 개발 머신이 Metal이라 검증 수단 없음 |
| 효과 실증 실험 (다암 비교·32K 측정) | §1-1 |

### 2-2. 사용자 결정 기록 (2026-07-20 브레인스토밍)

1. **무게중심 = 하네스 정직성 축** — 탐색형 지원·파일럿 2회차 선행을 제치고 선택
2. **거부 vs 알림 = 알림 전면 + 거부는 파이프만** — 근거는 M10 arm-block 실측
   (강요는 실패를 전이시킨다)과 [[small-model-native-strategy]]의 "강요 vs 유도"
3. **추가 묶음 = 4건 전부 채택** — 2-5 편집 diff · 1-1(b)(c) pack · `pilot.sh` 4건 · 계측 부채 §4
4. **측정 = 회귀 게이트 1배치**

컨트롤러가 브레인스토밍 중 후보 2-5를 C축에서 A축으로 재분류했다. 문서상
분류는 "탐색형 지원"이지만 실체는 *"상태선이 `files edited: 1`로 없던 진척을
접지했다"* — 같은 거짓 초록불 계열이다. 사용자가 이 재분류를 승인했다.

## 3. 축 A — 검증 접지 상위층

### 3-1. 착수점 실측

브레인스토밍과 1R 리뷰에서 코드·라이브 서버로 확인한 사실이다. 설계가 여기
의존하므로 구현 전에 재확인할 것. **1R 리뷰가 초판 6행을 전수 검증해 6/6
정확 판정했다** — 아래 두 행은 리뷰가 추가로 실측한 것이다.

| 사실 | 위치 | 설계 영향 |
|---|---|---|
| `has_unquoted_pipe`가 **비공개** | `src/tools/run_command.rs:16` | 가시성 승격 필요 |
| `StatusNote::record_command_result(exit, summary)`가 **명령 문자열을 안 받음** | `src/agent/status_note.rs:73` | 시그니처 변경 필요 |
| `VerifyOk` 무장 판단이 **`agent/mod.rs`의 이벤트 구성 지점**에 있음 | `src/agent/mod.rs:456~498` | `finish_nudge.rs`는 **안 건드려도 된다** |
| `render_diff`가 **이미 호출됨** — 전부 `preview()` 안 | `edit_file.rs:361`, `write_file.rs:46·51` | A-3은 "만든 것을 결과에도 붙인다" |
| `MAX_DIFF_LINES = 120` | `src/tools/diff.rs:3` | 승인 게이트용. 모델 채널엔 **별도의 더 작은 상한** 필요 |
| `ResponseMessage` = `role` + `content`뿐 | `src/llm/types.rs:39` | B-1의 수선 대상. `Delta`(`:73`)가 아니다 |
| `push_user_request`(꼬리 user면 병합) / `record_extra`(트랜스크립트만) **실재** | `session.rs:95` / `:104` | §4-2(b)의 부작용 처리가 성립 |
| **비스트리밍 `choices[0].message`에도 `reasoning_content`가 온다** (라이브 실측: `finish_reason=length`, content 0자, `reasoning_content` 127자) | — | B-1의 표적·필드명 판정이 옳음. 후보 문서 증거는 *스트리밍* 델타였으므로 이 확인이 필요했다 |
| **VERIFY_NUDGE 해제가 종료 코드를 안 본다** (코드 주석이 명시: *"해제 조건이었던 'Ok이면 종료코드 무관'에서 공허 런만 제외"*) | `agent/mod.rs:466~471` | §3-3-2의 전제 |
| `verification_line()`이 **한 줄 반환 계약**, 두 렌더 분기가 그 위에 섬 | `status_note.rs:99~121` | §3-4의 제약 |

### 3-2. 왜 "긍정 증거 요구"로 전면 뒤집지 않는가

가장 단순해 보이는 설계는 검증 성립 술어를 뒤집는 것이다 — `parse_test_summary`가
`ran > 0 ∧ failed == 0`을 실제로 확인했을 때만 검증으로 친다. 이러면 다섯 형태
중 넷이 한 번에 덮인다.

**이 안을 기각한다. `parse_test_summary`는 libtest 전용이기 때문이다.**

loco는 Rust 전용 도구가 아니다. `run_command`는 `make`·`npm test`·`pytest`
무엇이든 실행하고, 그 출력에는 `test result:` 줄이 없다. 긍정 증거를 요구하면
비-Rust 레포에서 **검증이 영영 성립하지 않고** VERIFY_NUDGE가 매 런 발동한다.
M12가 `None`을 보수적 폴백으로 둔 이유가 정확히 이것이며, 그 판단은 옳았다.

따라서 거부 술어에 넣을 수 있는 것은 **언어 무관하게 판별 가능한 사실**뿐이다.
파이프가 그 조건을 만족한다. 종료 코드도 언어 무관하지만, 그것을 거부 조건으로
쓰는 것은 "실패한 검증도 검증"이라는 기존 거래를 뒤집는 일이라 §3-3-2에서
명시적으로 범위 밖에 둔다.

### 3-3. A-1 · 파이프 가드를 거부 술어로

`has_unquoted_pipe`를 `pub(crate)`로 승격하고, `agent/mod.rs`의 단일 지점
(`cmd_exit`/`cmd_summary`/`empty_verify` 계산부)에서 다음 둘 **양쪽**에 가드를 건다.

- `finish_nudge::TurnEvent::VerifyOk` 무장
- `VERIFY_NUDGE` 해제 (`mutated_since_verify = false`)

즉 **파이프가 있는 `run_command` Ok는 검증으로 치지 않는다.**

이 수선의 논거는 새 정책 도입이 아니라 **자기모순 해소**다. M11이 이미 파이프
명령의 Done 결과에 "이 exit code는 파이프라인 마지막 명령의 것"이라는 출처
안내를 붙이고 있다. 그런데 정작 하네스 자신은 같은 턴에 그 exit 0을 검증으로
세고 있었다. 모델에게 믿지 말라고 알린 신호를 하네스가 믿고 있었던 것이다.

**덮는 범위**: #1(5건) · #3(`tail` 절단, 1건) · #2 중 파이프 기인분.

#### 3-3-1. VERIFY_NUDGE 문구를 반드시 함께 고친다 (리뷰 C3)

**이것은 A-1의 선택 사항이 아니라 구성 요소다.** 현행 문구는
(`agent/mod.rs:70`):

> `"You modified files but never ran a verification command. Run the project's tests (e.g. cargo test) with run_command, then finish."`

파이프 가드가 발동하면 이 문구는 **파이프로 `cargo test`를 방금 돌린 모델에게
"never ran a verification command"라고 말한다.** 거짓이고, 파이프를 지목하지도
않는다. "정직한 검증 신호"라는 이름의 마일스톤이 하네스에 새 거짓말을 심는
셈이다.

예상되는 모델 반응도 자명하다 — "테스트를 돌리라"는 지시를 받았으니 **같은
파이프 명령을 다시 돌리고**, `RepetitionTracker`의 동일 호출 창에 들어가 반복
교정/정지로 간다. **§9 R1이 걱정하는 풍선효과의 가장 그럴듯한 구체적 경로가
이것이며, 문구를 고치지 않으면 R1은 관측 대상이 아니라 예정된 결과다.**

따라서 파이프로 거부된 경우 **별도 문구**를 낸다. 대의는 *"검증 명령을 돌리긴
했지만 셸 파이프라인이라 그 종료 코드가 테스트 통과 여부를 말해주지 않는다.
파이프 없이 다시 돌린 뒤 finish하라"*이며, 정확한 영문은 §11 열린 질문 1.

구현상 이는 "마지막 `run_command`가 파이프였는가"를 런 스코프로 기억해야 한다는
뜻이다(현행 `verify_nudged` 래치와 같은 수명).

#### 3-3-2. A-1이 바꾸지 않는 것 — 해제 술어는 종료 코드를 안 본다 (리뷰 I5)

`agent/mod.rs:466~471`은 `run_command`가 Ok이기만 하면(공허 런 제외)
`mutated_since_verify = false`로 놓는다. **종료 코드를 전혀 보지 않으며, 이는
의도된 동작이다** — 코드 주석이 *"해제 조건이었던 'Ok이면 종료코드 무관'에서
공허 런만 제외"*라고 명시하고, `agent/mod.rs:1850`의 테스트가 타임아웃 경로에
대해 이를 고정한다.

따라서:

> **A-1은 여기에 파이프 조건만 더한다. 비-파이프 실패 검증(exit 101 등)이
> 여전히 VERIFY_NUDGE를 해제한다는 사실은 바꾸지 않는다.**

#2("컴파일 실패인데 검증으로 접수", 3건) 중 **파이프가 아닌 분량은 A-1로
덮이지 않는다.** A-2의 상태선이 그 사실을 알리지만 상태선은 알림 채널이라
finish를 막지 않는다. 이것을 바꾸는 것은 §3-2가 기각한 "긍정 증거 요구"에
해당하므로 §2-1에 범위 밖으로 명시했다. **M14 종료 후 "#2를 덮었다"고 쓰지 말 것.**

#### 3-3-3. 파이프 턴의 이벤트 매핑 — `Other`로 결정 (리뷰 C3)

`finish_nudge`에는 두 선택지가 있고 스펙이 고르지 않으면 구현자가 임의로 정하게
된다. M12는 공허 런에 대해 **의도적으로** `VerifyOther`를 골랐고 주석에 이유를
박아 뒀다(`agent/mod.rs:494`: *"VerifyOther로 떨어뜨려 기존 무장까지 내린다"*).

**파이프 턴은 `TurnEvent::Other`(상태 불변)로 떨어뜨린다.**

근거: `VerifyOther`의 `disarm()` 의미론은 "검증이 실패했으니 finish 유도를
거두라"는 것이다. 파이프는 **"검증이 실패했다"는 증거가 아니라 "알 수 없다"는
증거**다. `VerifyOther`를 고르면 정상 `cargo test`로 이미 무장한 상태가 뒤이은
`cargo test | tail` 한 번에 해제되어 **FINISH_NUDGE 도달범위가 M12 대비 후퇴**하고,
그 후퇴는 §8 관측 항목(finish 누락 스트릭)에 나타나되 원인을 소급 추정하게 된다.

#### 3-3-4. 정당한 파이프 검증의 처리

`cargo test 2>&1 | tail -50`은 실제 검증 의도이지만 이 가드에 걸린다. 설계상
수용하며 근거는 셋이다.

1. **`VERIFY_NUDGE`는 런당 1회 래치**라 finish를 영구 차단하지 않는다.
   1R 리뷰가 코드로 확인했다 — `agent/mod.rs:200` `verify_nudged = false` →
   `:304` `if mutated_since_verify && !verify_nudged`, 리셋 경로 없음
2. **거부 시 파이프를 지목하는 이유가 전달된다** — 단 §3-3-1을 구현한 경우에만.
   초판은 현행 문구가 이유를 전달한다고 썼는데 **틀렸다**(리뷰 C3)
3. **파이프로 절단된 출력은 실제로 하네스가 검증 여부를 알 수 없는 출력이다** —
   가드가 참을 말하고 있다

**그럼에도 풍선효과는 실재하는 위험이다.** §8 관측 항목과 §9 R1에 고정한다.

### 3-4. A-2 · 알림 전면

`StatusNote::record_command_result`가 명령 문자열을 받도록 시그니처를 넓히고,
상태선의 verification 줄이 다섯 형태를 **정직하게** 렌더한다.

원칙: **언어 무관한 사실만 적는다.** libtest 파싱이 `None`이어도 파이프 여부와
"요약이 없다"는 사실은 알 수 있다.

#### 3-4-1. 줄 수 제약 — 반드시 한 줄 안에 (리뷰 C1)

**초판이 "사실 한 줄을 더한다"고 쓴 것은 M11 상태선 블록 계약을 깨는 지시였다.**
1R 리뷰가 실행으로 재현했고 컨트롤러가 코드로 독립 확인했다.

`verification_line()`은 **한 줄을 반환하는 계약**이고 두 렌더 분기가 그 전제
위에 서 있다(`status_note.rs:99~121`):

- 무뮤테이션 분기 — 전체를 **단일 줄**로 조인: `"{MARKER}files edited: none yet | {verification} | {turns_line}"`
- 뮤테이션 분기 — `"...\n{CONT_INDENT}{verification}\n{CONT_INDENT}{turns_line}"`.
  `verification` 안에 `\n`이 들어가면 **그 둘째 줄에는 `CONT_INDENT`가 안 붙는다**

블록 계약("마커 줄 + 9칸 들여쓴 연속 줄")을 벗어난 줄은 `session.rs:156`
`remove_status_note()`가 걷어내지 못한다. 결과는 **영구 잔존 + 케이던스
(5/10/15/20)·pacing마다 누적**이다. 리뷰의 프로브 실행에서는 `turns:` 줄까지
고아 줄에 딸려 나가 제거 후에도 남았다.

이것이 왜 Critical인가: **누적되는 고아 줄이 컨텍스트를 조용히 먹는데, 그것이
바로 같은 마일스톤의 B-2가 고치려는 문제다. A-2가 B-2를 상쇄한다.** 게다가
게이트 배치는 이 회귀를 잡지 못한다 — 통과율에 즉시 안 나타나고,
`exp_metrics.py`의 status 마커 카운트는 마커 줄만 세므로 고아 줄은 계측되지 않는다.
그리고 **현행 스위트 370건 중 이 불변식을 지키는 테스트가 0개다**(리뷰가 A-2
형태의 둘째 줄을 넣고 전체 스위트를 돌려 `370 passed` 확인).

**따라서 설계 제약은 다음과 같다.**

- 추가 사실은 **규칙 5의 한 줄 안에** 넣는다. 예:
  `verification: last command exited 1 (no test summary in output)`
- 여러 줄이 불가피하면 `verification_line()`이 `Vec<String>`을 반환하도록 바꾸고,
  두 렌더 분기가 각각 ` | ` 조인 / `\n{CONT_INDENT}` 조인을 **책임진다**
- **§7에 불변식 테스트를 수용 기준으로 올린다** — *"모든 렌더 결과의 2번째 이후
  줄은 `CONT_INDENT`로 시작한다"*. 이 테스트가 0개인 것 자체가 별도 결함이다

#### 3-4-2. 무엇을 알릴 것인가

- **파이프** — exit code를 근거로 렌더할 때(현행 규칙 4·5) 그 exit code가
  파이프라인 마지막 명령의 것임을 표시
- **요약 부재** — `parse_test_summary`가 `None`이면 현행 규칙 5가 "last command
  exited N"만 렌더한다. 요약을 찾지 못했다는 사실을 같은 줄에 더한다.
  #2(컴파일 실패)와 #4(`--lib`을 bin 전용 크레이트에)가 여기 해당
- **공허 런** — #5(`ran==0 ∧ filtered_out==0`)는 **거부하지 않는다.** M12가
  `zero_ran_zero_filtered_out_is_a_real_verification_not_a_vacuous_run` 테스트로
  못박은 거래를 반전시키지 않고, 상태선이 침묵하지 않게만 한다

**M12 거래를 왜 유지하는가**: 파일럿이 그 거래의 반증(2건)을 제공한 것은 사실이다.
그러나 반전하면 테스트가 0건인 프로젝트·`cargo build`만으로 검증되는 과제에서
검증이 성립하지 않는다. 거래를 바꾸는 대신 **정보를 준다** — 사용자 결정 2의 직접 적용.

기존 5규칙과 **텍스트가 겹치지 않아야 한다**(M12가 규칙 3과 run_command 노트를
다른 문구로 둔 선례). 정확한 영문은 §11 열린 질문 1.

### 3-5. A-3 · 편집 결과를 모델에게

**결함의 실체는 "신호 부재"가 아니라 "승인자에게만 가고 모델에게는 안 감"이다.**
`render_diff`는 이미 `edit_file.rs:361`·`write_file.rs:46·51`에서 호출되어
`preview()`를 통해 승인 게이트에 뜬다. 모델이 받는 것은 `edit_file` 성공 시
±3줄 컨텍스트뿐이다.

파일럿에서 관측된 세 형태 전부가 이 비가시성의 산물이다.

- **조용한 삭제** — `search`가 실존 텍스트와 맞고 `replace`가 더 짧으면 정확히
  요청대로 지운다. 공개 상수 3개가 소실됐고 finish 요약은 추가만 보고했다
- **조용한 중복** — 앵커 기반 삽입은 멱등이 아니다. 재실행 시 앵커가 살아 있어
  또 1회 매치 → 테스트 사본 2개(E0428)
- **무효 편집이 진짜 편집으로** — `replace == search + "\n"`이 동일성 검사를
  통과해 `Edited (matched exact)`가 되고 상태선이 `files edited: 1`로 없던
  진척을 접지했다. 그 개행은 커밋됐다

#### 3-5-1. 예산은 중립이 아니라 유계다 (리뷰 I2)

**초판의 "기존 ±3줄을 대체하므로 예산 중립"은 두 방향으로 거짓이다.** 리뷰가
실측했다.

**거짓 A — `write_file`에는 대체할 ±3줄이 없다.** `write_file::run()`은
`"Wrote {} ({} lines)"` 한 줄만 반환한다(`write_file.rs:66`). `render_context`는
`edit_file.rs:381`에만 있다. 따라서 `write_file`에 diff를 붙이는 것은 **순수 추가**다.

**거짓 B — `edit_file`에서도 중립이 아니다.** `render_context`는 편집 크기와
무관하게 상수(한 구간)인 반면 `render_diff`는 헝크 수에 비례한다.

| 케이스 | `render_context` | `render_diff` |
|---|---|---|
| 단일 줄 편집 | 9줄 / 123B | 7줄 / 72B |
| 21줄 삭제 | 9줄 / 120B | **27줄 / 250B** |
| `replace_all` ×8 | 9줄 / 116B | **56줄 / 554B** |

하필 **조용한 삭제**(A-3의 주 표적)에서 3배, `replace_all`에서 6배다.

**정정된 서술**: 15줄 상한이면 편집 턴당 최대 ~15줄로 `render_context` ~9줄 대비
**약 1.7배**다 — **유계이되 중립이 아니다.** §9 R2가 이 위에 선다.

#### 3-5-2. 설계

성공한 `edit_file`/`write_file` 결과에 diff를 첨부한다.

- **모델 채널 전용 상한**을 새로 둔다. `MAX_DIFF_LINES = 120`은 승인 게이트용이며,
  그 값을 매 편집 턴에 붙이면 B-2가 고치려는 문제를 스스로 악화시킨다.
  15줄 안팎으로 하되 정확한 값은 §11 열린 질문 2
- **`edit_file`에서는 기존 ±3줄 컨텍스트를 대체한다**(추가가 아니다).
  `write_file`에는 대체할 것이 없으므로 순수 추가임을 인정하고 상한으로만 유계
- **`write_file`을 범위에 포함한다.** 파일럿의 최대 손실 사례(함수 시그니처 한
  줄 소실)가 `write_file` 전체 재작성에서 났고, `EDIT_STRATEGY_CORRECTION`이
  막힌 모델을 **바로 그 안전망 없는 경로로 유도**한다(후보 §3-4). 제외하면
  주 표적이 커버 밖이 된다.
  **단 신규 파일(`old`가 빈 문자열)은 전 줄이 추가 줄이라 신호가 0이므로 diff
  대신 현행 요약 줄을 유지한다**
- **절단 시 삭제 줄을 우선 보존한다.** 단 21줄 삭제 케이스에 15줄 상한을 대입하면
  삭제 줄만 남아 **무엇이 그 자리에 들어왔는지가 사라진다.** 최소 형태
  (삭제 N줄 / 추가 M줄 요약 + 실제 줄 일부)를 §11 열린 질문 2에 함께 올린다

## 4. 축 B — 확정 결함 티켓

### 4-1. B-1 · `reasoning_content` 미파싱

`src/llm/types.rs:39`의 `ResponseMessage`는 `role`·`content` 두 필드뿐이다.
llama.cpp가 Ornith의 사고 토큰을 `reasoning_content`로 분리해 흘리면 serde가
그것을 버린다. 파일럿의 "빈 응답" 4건(각 111~122초 생성, 포착 0자)이 전부
이것이고, 그 자리에 문자열 `"(empty)"`가 모델 히스토리에 주입된다.

**수선 대상은 `Delta`가 아니라 `ResponseMessage`다.** 에이전트 루프는
`stream: false`이므로 `Delta`에 필드를 넣으면 `/chat` 경로만 바뀌고 파일럿
세션은 하나도 안 바뀐다. 1R 리뷰가 라이브 서버로 **비스트리밍
`choices[0].message`에도 `reasoning_content`가 온다**는 것을 확인했다(§3-1) —
후보 문서의 증거는 스트리밍 델타였으므로 이 확인이 필요했다.

**기대 효과를 낮춰 잡는다 — 이것은 교정이 아니라 진단이다.** 이 수선은 4개 턴
중 어느 것도 되살리지 못한다. `finish_reason: length` + `content` 공백은 모델이
예산을 전부 추론에 쓰고 JSON을 한 글자도 못 냈다는 뜻이라, 잘린 산문을
`parse_turn`에 넣으면 length 실패가 파싱 실패로 바뀔 뿐이다.

**`usage` 노출의 정확한 서술**(리뷰 M3 — 라이브 실측): 서버 응답은
`{"completion_tokens":40,"prompt_tokens":23,"total_tokens":63,...}` 형태로
**`reasoning_tokens` 같은 분해 필드가 없다.** 확인한 턴은 content 0자 /
`reasoning_content` 127자인데 `completion_tokens`는 40 — 추론 토큰이
`completion_tokens`에 **포함**되어 있다. 따라서 산출물은
*"`usage.completion_tokens`를 노출한다. content가 빈 턴에서는 그 값이 곧 추론
소비량이고, 그 외에는 합산값이라 분리되지 않는다"*이다. `ChatResponse`의
`usage` 파싱이 현재 없으므로 함께 추가한다.

**수용 기준 — 반증 가능해야 한다**: `max_output_tokens`를 추론이 끝나기 전에
잘리는 값으로 강제로 낮춰 `finish_reason == "length"` ∧ `content` 공백을 **먼저
재현**하고, 그 턴의 `.loco/sessions/*.jsonl` assistant 행이 `"(empty)"`가 아님을
단언한다. **"에이전트 턴 1회를 돌려본다"만으로는 안 된다** — 정상 턴은 그 경로에
도달하지 않으므로 결함이 그대로 있어도 통과한다([[verification-criteria-must-be-falsifiable]]).

### 4-2. B-2 · `pack()` 데이터 손실

`src/session.rs`의 쌍 삭제 `drain(1..=2)`가 사용자 과제 메시지를 제거한다.
세션 1(Z1)은 예산 3686에 대해 3751(65 초과)로 축약 대상이 없어 쌍 삭제가
발동했고, **user 과제 + assistant blob이 함께** 사라졌다. 이후 과제 부재가
영구적이고 모델은 한국어 과제에 영어로 되물으며 끝났다.

**교란 요인을 먼저 적는다**: 심각도는 파일럿 자신의 `max_output_tokens=4096`
탓이다. 기본값 2048이면 예산 5529로 이 세션은 통과하고 `pack()`이 발동하지
않는다. **기본 설정에서는 재현되지 않는다.** 결함은 실재하지만 우선순위 근거를
"파일럿에서 N세션"으로 쓰면 안 된다.

**(c) — `max_output_tokens` 상향 시 `input_budget` 붕괴 방지.** 하한을 두거나
경고한다. **가장 싸고, (a)·(b)가 못 덮는 경우까지 덮는다.** 이 항목을 먼저 한다.

**(b) — `finish_reason: length` 절단 blob을 히스토리에 push하지 않는다.**
세션 1을 단독 해결한다. 문자 그대로 구현하면 부작용 2건이 **둘 다** 발생하므로
설계에 포함한다.

1. **role 교대 위반** — assistant push를 생략하면 `[system, user 과제, user 회복]`으로
   user가 연속된다. 교정 담당 `merge_adjacent_same_role`는 `pack()`의 쌍 삭제 루프
   안에서만 호출되는데 (b)의 목적이 바로 pack 미발동이라 **영영 교정되지 않는다.**
   → `push`가 아니라 `push_user_request`(`session.rs:95`, 꼬리가 user면 병합)를 탈 것
2. **트랜스크립트 증거 소멸** — `Session::push`는 `transcript.record`와
   `messages.push`를 함께 한다. push를 생략하면 절단 blob이 **JSONL에서도**
   사라진다. M13의 디코딩 퇴화 분석은 전부 그 blob을 읽어서 만든 것이다.
   → `record_extra()`(`session.rs:104`)로 트랜스크립트는 보존할 것

#### 4-2-1. B-1과의 충돌 해소 — 최종 상태를 하나로 (리뷰 C2)

**초판은 B-1과 B-2(b)가 `agent/mod.rs:239~242`의 동일한 분기를 서로 반대로
요구한다는 사실을 한 번도 언급하지 않았다.**

```rust
if resp.finish_reason() == Some("length") {
    let t = resp.text();
    session.push(ChatMessage::assistant(if t.is_empty() { "(empty)" } else { t }));
```

- B-1은 *"`"(empty)"` 리터럴 대신 추론 꼬리를 **히스토리에**"*를 산출물로 든다
- B-2(b)는 *"length 턴 blob을 **히스토리에 push하지 않는다**"*를 요구한다
- **발동 조건이 정확히 같다**(`finish_reason == "length"`). 파일럿의 빈 응답 4건은
  전부 이 경로다

방치하면 M13 2R가 지목한 "데이터 본 뒤 편한 쪽 고를 seam"이 된다 — 구현자가
B-2(b)를 따르면 B-1의 산출물이 사라지는데, **B-1의 수용 기준은 그래도 통과한다**
(`record_extra`로 라우팅하면 되므로). 기준은 통과하는데 선언한 산출물은 없는
상태가 조용히 성립한다.

**최종 상태를 다음 하나로 못박는다.**

> length 턴에서 절단 blob은 **히스토리에 push하지 않고**(B-2b),
> `record_extra("assistant", …)`로 **트랜스크립트에만** 남긴다.
> B-1의 `reasoning_content` 파싱은 **그 트랜스크립트 레코드의 내용**을
> `"(empty)"`에서 추론 꼬리로 바꾼다.

즉 B-1의 산출물 (a)는 "히스토리에"가 아니라 **"트랜스크립트에"**다. 두 축이
합류하며, §4-1의 수용 기준(`.loco/sessions/*.jsonl` assistant 행이 `"(empty)"`가
아님)은 그대로 유효하다 — 애초에 트랜스크립트를 보는 기준이었다.

**구현 순서를 못박는다: B-2(b)를 먼저, B-1을 그 위에**(§10).

#### 4-2-2. (a)는 착수 금지

인덱스 1 고정은 이 마일스톤에서 하지 않는다. 실측 확인: 쌍 삭제 루프
(`session.rs:139~146`)에는 마지막 메시지 보호가 없다(축약 루프 `:125~126`만 `last`를
계산한다). 인덱스 1을 고정하고 드레인을 옮기면 ① 역할 술어도 함께 옮겨야 하고
안 그러면 `else` 분기의 `remove(1)`가 고정한 과제를 지운다 ② 세션 1의 형태
(`len 4`)에서 인덱스 3은 **현재 요청**이라 그것을 지운다. 종료성·마지막 메시지
보호 설계가 선행돼야 한다.

**수용 기준**: `max_output_tokens`를 크게 올린 설정으로 세션 1의 형태를 재현해
**셋을 함께** 단언한다 — ① 과제 메시지가 히스토리에 살아남는다 ② 트랜스크립트에
절단 blob이 남아 있다 ③ role 교대가 깨지지 않는다.

### 4-3. B-3 · `schema_fallback_count` 집계

`Report`/`TaskReport`에 `schema_fallback_count`를 더한다. 지금은 앵커 게이트가
`tasks[].runs[].schema_fallback`을 전수 순회해야 하는데, 그 스크립트를 잘못
짜면(첫 런만 본다든지) **fail-open 위험이 Rust에서 스크립트로 이동할 뿐이다.**
M6이 `_count` 접미사 집계를 도입한 이유가 정확히 이것이다. 기존 키 불변의
가산 변경.

### 4-4. B-4 · 거부 경로 `!stop` 가드 회귀 테스트

**가드는 하나가 아니라 둘이다**(리뷰 I3). `agent/mod.rs:411`(finish_nudge 억제)과
`:415`(status 억제). 1R 리뷰가 **각각 독립적으로 제거해 전체 스위트를 돌렸고 둘 다
`370 passed; 0 failed`** — 둘 다 무방비다. 초판의 "테스트 1개로 닫힌다"는 틀렸다.

- **status 가드** — 테스트로 닫는다
- **finish_nudge 가드** — **도달 불가일 가능성이 높다.** 거부 경로의 이벤트는
  둘뿐이고(`:404~406`), `MutationAttempt`는 `disarm()`을 호출해 `armed=false`로
  만들며 발동 조건이 `self.armed && …`이므로 발동할 수 없다. `Other`는 상태를
  바꾸지 않으므로 그 턴에 새로 조건이 참이 될 수 없다(참이었다면 직전 `on_turn`에서
  이미 발동·래치됐다). **단 리뷰어가 이를 코드 추론으로만 제시했고 실행으로
  증명하지 않았다.** 구현 태스크는 (a) 도달 불가를 증명하면 코드 주석으로 못박고
  가드를 남기거나 (b) 증명하지 못하면 테스트를 작성한다. **작성 시도가 실패할 수
  있음**을 §11 열린 질문 3에 올린다

§7의 수용 기준은 **두 가드를 모두 지목하도록** 좁힌다 — 안 그러면 status 가드
하나로 문자 그대로 만족되어 "기준은 통과했는데 공백은 절반 남은" 상태가 된다.

## 5. 축 C — 도구·계측 부채

### 5-1. `pilot.sh` 4건

다음 파일럿에서 처음 쓰이므로 지금 고쳐 둔다. 전부 실재 확인됨(리뷰 N3).

- **cwd 전제**(`:19`) — `REPO="$(pwd)"`인데 `git diff "$START_REV"`는 레포 전체를
  본다. 서브디렉터리 실행 시 어긋난다. `git rev-parse --show-toplevel`로 고정
- **타임아웃 환경변수 검증**(`:17~18`) — 아래 5-1-1
- **세션 전 `read` 4개의 EOF 내성**(`:96,102,104,106`) — 종료 코드 1이 실제 오류와
  구별되지 않는다. 한 줄 안내 + 주석에 "의도적 제외" 명시. 세션 후(`:326,338`)는
  이미 `if ! read`로 방어됨
- **INT 트랩**(`:61`) — 포그라운드 자식 대기 중 프로세스 그룹 SIGINT는 트랩을
  실행시키지 않는다(bash 3.2 = macOS `/bin/sh`). 결과적 동작은 바람직하므로
  **동작 변경이 아니라 문서화**로 처리하고, 세션 중 INT까지 잡는 `wait` 기반
  구조는 다음으로 미룬다

#### 5-1-1. 타임아웃 검증 — 초판의 "(실측)"이 거짓이었다 (리뷰 I1)

초판은 후보 문서를 그대로 옮겨 *"비숫자 값(`600s`·`10m` 같은 오타)이면 `sleep`이
즉시 실패해 상한이 조용히 사라진다(실측)"*고 썼다. **인용된 두 예시가 둘 다
틀렸다.** macOS `/bin/sleep`은 단위 접미사를 지원한다(`usage: sleep number[unit]`,
unit = `s`/`m`/`h`/`d`). 1R 리뷰가 `/bin/sh`로 직접 돌려 확인했다(macOS 26.5.2):

| 값 | 결과 |
|---|---|
| `600s` · `10m` · `1e3` | **수용됨** — 상한 유지 |
| `abc` · `600 `(후행 공백) · `-5` · 빈 문자열 | 즉시 rc=1 → **상한 소멸** |

**기전 자체는 참이다** — `run_with_timeout()`의 서브셸 안 `sleep "$_rwt_timeout"`이
죽으면 `kill_tree`가 영영 안 돈다. 틀린 것은 증거다.

**그리고 진짜 결함은 초판 서술보다 나쁘다**: `10m`은 실패하지 않고 **조용히
600초로 해석된다.** 변수 이름이 `..._SECS`인데 값이 분 단위로 먹히고 아무 경고도
없다. **"상한이 사라진다"가 아니라 "상한이 60배로 조용히 바뀐다"**가 이 예시의
실제 실패 모드다.

따라서 `case`는 **실패 방지가 아니라 숫자만 허용**이어야 한다 — `600s`·`10m`·`1e3`도
거부한다. 제안된 수선 자체는 그대로 옳다.

**이 항목이 남기는 교훈**: 이 절은 바로 아래 문단에서 M13의 BSD `find` 사고를
인용하며 *"도구는 그 도구가 도는 환경에서 검증하라"*고 경고한다. **경고를 적은
그 절이 같은 실패를 저질렀다** — §12-5의 승계 규율이 스펙 자신에게 적용되지
않은 사례다. 후보 문서(`docs/m14-candidates.md` §4-2)도 같은 오류를 담고 있으므로
구현 태스크에서 함께 정정한다.

**검증 규율**: `pilot.sh`는 `/bin/sh`로 돈다. 대화형 셸의 함수·별칭·PATH가
검증을 무효화할 수 있다([[verification-environment-shadowing]]).
**`/bin/sh scripts/pilot.sh` 형태로 직접 돌리고, 외부 명령은 `/usr/bin/`의 실제
바이너리로 확인할 것** — §7 수용 기준에 올린다.

### 5-2. 트랜스크립트 영속화

M13이 남긴 계측 불가 항목 중 둘을 닫는다. 둘 다 실재 확인됨(리뷰 N2).

- **`finish_reason`이 트랜스크립트에 영속되지 않는다** — `agent/mod.rs:239`(판정)과
  `:699`(테스트 헬퍼)에만 나온다. `length` 턴 수를 직접 셀 수 없어 M13은 빈-content
  턴을 대리 지표로 썼다. B-1·B-2(b)와 같은 방향이다
- **컨텍스트 오버플로가 트랜스크립트에 안 남는다** — `AgentEvent::Notice`가
  `agent/mod.rs`에 19회 등장하지만 그 파일에 `transcript`/`record_extra` 호출이
  0건이다. M13 파일럿 표에서 이 행은 **0이 아니라 미측정**이었고, M13 스펙 리뷰의
  C1이 정확히 이 공백 때문에 "배치는 정상 종료했는데 흔적 0"인 시나리오를 만들어냈다

### 5-3. `exp_metrics.py`

- **출력 형식** — `summary`의 `parse_fail_first`만 한글 키 + 서술문이라(`:350`)
  grep 레시피가 깨진다. 다른 전 필드처럼 영문 `key=value`로. CLAUDE.md의
  "identifiers는 영문" 관례와도 정합
- **신규 마커 카운트** — A-1 파이프 거부 발동 · A-3 diff 첨부 · A-2 신규 상태선
  사실 줄. **장치가 발동조차 하는지를 게이트 배치에서 관측**하기 위한 것이며,
  효과 지표가 아니다(§1-1). M12의 `args_tool_key`(배치에서 118건 발동, 오발동 0
  전수 확인)가 이 방식의 전례. **단 §3-4-1의 고아 줄은 이 계측으로 잡히지 않는다**
  (마커 줄만 세므로) — 그래서 그쪽은 단위 테스트 불변식으로 막는다

**드리프트 주의**: `exp_metrics.py`는 Rust 상수·술어를 손으로 복사한다
(`MAX_SR_CORRECTIONS`·`BADARGS_KEY_PREFIX`·`normalize`). 자동 검출이 없으므로
Rust 쪽을 고치면 **수동 미러가 필수**다(M12 최종 리뷰가 standing risk로 수용).

## 6. 축 D — 봉투 명시 (코드 변경 0)

후보 2-1은 수선이 아니라 범위 결정이었고, 사용자 결정은 **"명시하되 거절/경고
코드는 넣지 않는다"**이다. README 또는 `docs/`에 loco가 서비스하는 과제 봉투를
서술한다. 아래 수치는 1R 리뷰가 `docs/baselines.md` M13과 전수 대조해 일치 확인했다.

- **경로 지정 과제가 성공 봉투다** — 파일럿 13세션 중 성공 7(감사 전 최대치),
  경로 미지정 7세션은 0/7
- **성공 봉투의 크기** — 무경합 성공 4건의 diff 추가줄은 +0~23이고, 추가줄 29
  이상인 4건 중 무경합 성공은 0건이다

**증거 강도 상한을 반드시 병기한다.** 이 축은 사후 분석이다. `task_type`은 세션
전에 수집됐지만 "파일 경로 지정 여부"라는 분해는 사전등록되지 않았고, 20과제 중
19개를 컨트롤러가 설계했다. **"이 유형이 실패한다"와 "내가 설계한 이 유형의 과제
7개가 실패한다"를 이 데이터로 구분할 수단이 없다.** n=7 vs 13. 사례로 읽어야 한다.

**분모 규칙 병기 의무**: 성공 7은 무효 Z3와 감사 강등된 F5·R5를 포함한 감사 전
최대치이고, 같은 규칙을 적용하면 무경합 기준 4/12다. 0/7 쪽은 어떤 완화도 받지
않으므로 두 수치를 나란히 쓸 때는 반드시 병기한다.

**금지 서술**: *"합성 세트에서 이미 이 축만 흔들리고 있었다"*고 쓰지 말 것 —
1차 자료가 반증한다. 축 안 29/30(96.7%) vs 축 밖 41/42(97.6%)이고, 엄격 기준으로는
축 밖이 오히려 더 나쁘다(29/30 vs 40/42). **합성 세트는 이 축을 구별하지 못하며,
대비를 만드는 것은 실레포 쪽 0/7이다.** 또한 합성 5과제 중 2개는 "기전 제공"(cargo
실행 지시)인데 파일럿 7세션에는 기전을 주는 것이 없으므로, `29/30`을 단독 인용하면
"기전 있음 vs 없음"을 "경로 있음 vs 없음"으로 읽게 된다 — `17/18`(심볼만 주는 3과제)이
더 엄밀한 대응임을 병기한다.

## 7. 성공 기준

**게이트 4종** (전부 필수):

1. `cargo test` 전건 통과
2. `cargo clippy --all-targets -- -D warnings` 무경고
3. `cargo run -- eval tasks --verify` 12/12 · `eval tasks-large --verify` 3/3
4. `python3 scripts/exp_metrics.py --selftest` ok · `scripts/pilot_tally.py` 동작

**수용 기준 5종** (전부 반증 가능한 형태):

| # | 기준 | 이 수선이 없으면 실패하는가 |
|---|---|---|
| 1 | **상태선 블록 계약 불변식** — 모든 렌더 결과의 2번째 이후 줄이 `CONT_INDENT`로 시작한다 | 예 — 현행 스위트에 이 테스트가 0개이고, A-2를 소박하게 구현하면 즉시 실패한다(리뷰가 실행으로 확인) |
| 2 | **B-1** — `finish_reason == "length"` ∧ `content` 공백을 **먼저 재현**한 뒤 트랜스크립트 assistant 행이 `"(empty)"`가 아님을 단언 | 예 — 정상 턴은 이 경로에 도달하지 않으므로 재현이 선행돼야 한다 |
| 3 | **B-2** — 과제 생존 · 트랜스크립트 blob 잔존 · role 교대 무손상 **셋 다** | 예 — 부작용 2건 중 하나만 빠뜨려도 셋 중 하나가 깨진다 |
| 4 | **B-4** — `agent/mod.rs:411`·`:415` **두 가드를 각각** 제거하면 실패하는 테스트(또는 `:411`의 도달 불가 증명) | 예 — 현재는 각각 제거해도 370 passed |
| 5 | **`pilot.sh`** — `/bin/sh scripts/pilot.sh` 형태로 직접 실행해 확인 | 예 — 대화형 셸에서는 M13의 `find` 사고처럼 통과할 수 있다 |

**측정**: 회귀 게이트 배치가 사전등록 임계값을 충족 (§8).

**주장하지 않는 것** (§1-1 재확인): 게이트 통과는 §8-1이 정한 해상도 안에서의
무회귀 증거일 뿐이며, 검증 날조가 줄었다는 증거가 아니다.

## 8. 측정

회귀 게이트 1배치. **사전등록 필수** — `docs/experiments/PROTOCOL.md`.

| 항목 | 값 |
|---|---|
| 세트 | `tasks/` 스포트 (12과제 × 3반복 = 36런) |
| 대조 | `20260719T093254Z` — 35/36, 엄격 35 (**M13 게이트 배치**) |
| 임계값 | **사전등록에서 확정** — 아래 두 관례가 다른 수를 준다 |
| 재측정 | **1회 사전 공약.** 재측정도 임계 미달이면 비병합·정지·사용자 보고 |

**임계값 관례가 하나가 아니다**(리뷰 M1). 초판은 *"≥33/36 (M12·M13 관례)"*라고
썼는데 부정확하다. `docs/baselines.md` 대조 결과:

- **M12** — 고정 `≥33/36` (`:676`, "임계값에 정확히" 통과)
- **M13** — `게이트 총합 ≥ 앵커 − 4` (`:788`). 대조 35에 적용하면 **31**

사전등록에서 어느 쪽을 채택하는지와 근거를 적는다. 컨트롤러 권고는 M12 방식
(`≥33`)이다 — M13의 "앵커 −4"는 *서빙 스택 전환의 동등성* 판정용이었고 M14는
동일 스택의 회귀 게이트라 더 조이는 쪽이 맞다.

**대조 선택 근거**: `20260719T093254Z`는 파일럿과 같은 코드로 돈 배치다. 앵커
(`20260719T082030Z`, 35/36 엄격 34)가 아니라 이쪽이 M14 직전 상태다.

### 8-1. 이 게이트의 검출력 (리뷰 M2)

36런 이항, 임계 ≥33, 재측정 1회 공약 포함:

| 실제 통과율 | 1회 통과확률 | 2회 중 1회 통과확률 |
|---|---|---|
| 0.972 (대조) | 0.982 | 0.9997 |
| 0.90 | 0.509 | **0.758** |
| 0.87 | — | **≈0.50** |
| 0.80 | 0.052 | 0.102 |

**이 게이트가 배제할 수 있는 것은 대략 통과율 80% 미만의 붕괴이며, 5~10%p 규모의
회귀는 배제하지 못한다.** 거짓 경보는 거의 없는 대신(대조가 정상인데 실패할 확률
재측정 포함 0.03%) 실제 회귀도 절반은 놓친다. 그리고 §3-4-1의 고아 줄처럼
**통과율에 나타나지 않는 회귀는 아예 사정권 밖**이다 — 그래서 §7 수용 기준 1이
게이트가 아니라 단위 테스트다.

### 8-2. 수행 규율 (M11~M13 승계)

- `setsid` 데몬화 — 하네스 백그라운드 60분 수명 상한([[harness-background-60min-cap]])
- 측정 중 `cargo build`/`test` 병행 금지 (CPU 경합이 타이밍 판정을 왜곡)
- `ls ${TMPDIR}/.cargo` 사전 점검 — 존재 시 수동 제거
- `.loco/config.toml`을 대조 배치의 `effective_config`에 정합시킬 것.
  **`--release` 금지** — 대조가 디버그 빌드다(M13 I3: 빌드 프로파일은 report.json에
  안 남아 나중에 발견 불가능)
- 러너 무인 수행 시 "통지 의존 금지, 종료는 exit code와 스탬프 디렉터리로 직접
  확인"(M10 운영 교훈)
- 러너 보고의 대조군 수치는 `report.json` 직접 대조([[loco-measurement-report-cross-check]])

### 8-3. 관측 항목 (게이트 아님, 판정에 쓰지 않음)

- A-1 파이프 거부 발동 횟수 · 오발동 여부(전수 확인) · **새 VERIFY_NUDGE 문구 발동 횟수**
- A-3 diff 첨부 횟수 · 절단 발생 횟수
- **풍선효과 감시**: `finish` 누락 스트릭 · 거짓 finish 수 · `stop_cause` 분포 ·
  **동일 파이프 명령 재실행으로 인한 반복 정지**(§3-3-1이 막으려는 것)

## 9. 리스크

| # | 리스크 | 대응 |
|---|---|---|
| R1 | **파이프 가드 풍선효과** — 정당한 `cargo test \| tail`도 미검증 처리 → 실패가 finish 규율로 전이(M10 arm-block 전례) | §3-3-1(문구 갱신)이 **필수 대응**이다. 그것 없이는 R1이 관측 대상이 아니라 예정된 결과다(리뷰 C3). 더해 §8-3에 재실행 반복 정지를 관측 항목으로 고정 |
| R2 | **A-3 diff가 컨텍스트를 먹어 B-2를 악화** | 예산은 **중립이 아니라 유계**(§3-5-1, `edit_file` ~1.7배 / `write_file` 순수 추가). 모델 채널 전용 상한이 **필수 전제**이고 상한 없는 구현은 스펙 위반 |
| R3 | **A-2가 B-2를 상쇄** — 고아 줄 누적(§3-4-1) | 한 줄 제약 + §7 수용 기준 1의 불변식 테스트. 게이트로는 안 잡힌다 |
| R4 | B-1↔B-2(b) 충돌로 산출물이 조용히 사라짐 | §4-2-1이 최종 상태를 하나로 못박음 + §10이 순서를 고정 |
| R5 | `exp_metrics.py`의 Python↔Rust 손복사 드리프트 | Rust 수정 시 수동 미러 필수 (§5-3) |
| R6 | 검증 기준이 통과할 수밖에 없는 형태 | §7이 기준마다 "이 수선이 없으면 실패하는가"를 열로 강제([[verification-criteria-must-be-falsifiable]]) |
| R7 | `pilot.sh` 검증이 대화형 셸 환경에 오염 | `/bin/sh`로 직접 실행, §7 수용 기준 5 (§5-1) |
| R8 | **#2를 덮었다는 초과 주장** | §3-3-2가 A-1이 바꾸지 않는 것을 명시 |

## 10. 구현 순서 (고정)

**축 간 상호작용이 주 리스크이므로 순서를 스펙에서 못박는다**(리뷰 총평).

```
B-2(c) → B-2(b) → B-1 → A-1(§3-3-1 문구 포함) → A-2 → A-3 → B-3 / B-4 → C → D
```

근거:

- **컨텍스트를 먹는 축(A-2·A-3)이 컨텍스트를 지키는 축(B-2) 뒤에** 온다 — R2·R3
- **B-2(b)와 B-1이 인접**한다 — 같은 한 줄을 건드리므로(§4-2-1) 같은 작업자가
  연속 수행할 것. **분리 위임 금지**
- **A-1의 문구 갱신(§3-3-1)은 A-1과 분리 불가** — 별도 태스크로 쪼개면 그 사이
  상태가 §9 R1의 최악 시나리오다

## 11. 열린 질문 (구현 태스크에서 확정)

1. **모델 대면 영문 텍스트 3종** — ① A-2의 상태선 파이프 표시와 "요약 없음" 문구
   (기존 5규칙·run_command 노트와 텍스트가 겹치지 않아야 함) ② §3-3-1의 파이프
   전용 VERIFY_NUDGE 문구 ③ A-3의 diff 헤더
2. **A-3의 모델 채널 diff 상한 정확한 값**과 절단 시 최소 형태 — 21줄 삭제 케이스
   (§3-5-1)에서 삭제 줄만 남기면 무엇이 들어왔는지가 사라진다. "삭제 N줄 / 추가 M줄
   요약 + 실제 줄 일부"를 후보로 검토
3. **`agent/mod.rs:411` finish_nudge 가드의 도달 가능성** — 도달 불가를 증명하면
   주석으로 못박고, 못 하면 테스트를 쓴다. **작성 시도가 실패할 수 있다**(§4-4)
4. **B-2(c)의 형태** — 하한인가 경고인가, 임계값은 얼마인가
5. **§8 임계값 최종 확정** — M12 방식(`≥33`) vs M13 방식(`앵커−4`=31). 사전등록에서

## 12. 승계할 규율 (M13 §5)

이 마일스톤의 분석·리뷰에 그대로 적용한다. **1R 리뷰에서 5번이 스펙 자신에게
적용되지 않은 사례가 나왔다(§5-1-1) — 규율을 적은 절이 같은 실패를 저질렀다.**

1. **개입이 "궤적을 바꿨다"는 주장은 원장 판정과 diff 실물로 교차 확인한 뒤에만 쓴다**
2. **"성공"이 무엇의 성공인지 명시한다** — 도구 디스패치 Ok / 산출물 컴파일 /
   과제 충족 / 사용자 채택은 전부 다르다
3. **재계수는 귀인 오류를 못 잡는다.** 독립 리뷰 축에 "귀인"을 명시적으로 넣을 것
4. **자기가 새로 쓴 수치에 자기 규율을 적용한다** — 1R I4("15세션분" 합산)가 이것
5. **도구는 그 도구가 실제로 도는 환경에서 검증한다** — 1R I1이 이것
6. **"수정했다"는 그 수정에 대한 리뷰를 면제하지 않는다** — M13에서 리뷰 반영
   수정이 2라운드 연속으로 새 결함을 만들었다. **이 개정 1도 예외가 아니다**
7. **검증 기준은 실패할 수 있는지도 확인한다** — M13의 치명적 지적은 전부
   "확인했다고 생각했지만 안 돌려본 것"에서 나왔다

## 13. 리뷰 1R 처분 기록 (`cd093b1` 대상)

**전건 반영. 반박 0건.** 리뷰어가 뮤테이션 실험·라이브 서버 호출·이항 계산으로
검증했고, 컨트롤러가 Critical 3건을 코드로 독립 재확인했다.

| # | 지적 | 처분 |
|---|---|---|
| C1 | A-2 "한 줄 더한다"가 상태선 블록 계약을 깸 — 고아 줄 영구 잔존 | §3-4-1 신설(한 줄 제약 + `Vec<String>` 대안) · §7 수용 기준 1 신설 · §9 R3 신설 |
| C2 | B-1↔B-2(b)가 `agent/mod.rs:241` 한 줄을 반대로 요구 — seam | §4-2-1 신설(최종 상태 하나로 못박음) · §10 순서 고정 · §9 R4 |
| C3 | A-1이 VERIFY_NUDGE를 거짓말하게 만듦 + 이벤트 매핑 미결정 | §3-3-1 신설(문구 갱신을 A-1 구성 요소로) · §3-3-3 신설(`Other`로 결정) · §3-3-4 논거 ② 교정 · §9 R1 대응 격상 |
| I1 | `sleep` "(실측)"이 거짓 — `600s`/`10m`은 수용됨 | §5-1-1 신설(실측 표 + "60배 조용한 변경") · 후보 문서도 함께 정정하도록 지시 |
| I2 | "예산 중립"이 두 방향으로 거짓 | §3-5-1 신설(실측 표) · §3-5-2에서 `write_file` 범위 명시 결정 · §9 R2 |
| I3 | `!stop` 가드는 둘, 하나는 도달 불가로 보임 | §4-4 전면 개정(테스트 2개) · §7 수용 기준 4 · §11 열린 질문 3 |
| I4 | "15세션분"은 다중 라벨 합산 | §1 계수 주의 문단 신설 · §1-1에서 표현 삭제 |
| I5 | 해제 술어가 종료 코드를 안 봄 · #2 기전 서술 오류 | §3-3-2 신설 · §1 표 #2 기전 교정 · §2-1에 범위 밖 명시 · §9 R8 |
| M1 | 임계값 관례가 M12·M13이 서로 다름 | §8 표 + 두 관례 병기 · 권고와 근거 · §11 열린 질문 5 |
| M2 | 게이트 검출력 미명시 | §8-1 신설 · §1-1에 반영 |
| M3 | `usage`에 `reasoning_tokens` 분해 없음 | §4-1 정확한 서술로 교체 |
| M4 | 비스트리밍에도 `reasoning_content` 존재(긍정 확인) | §3-1 표에 행 추가 |
| N1~N4 | 확인 사항 | §5-1·§5-2·§5-3·§6에 위치 정보 반영 |
| 총평 | 분해 불필요, 단 구현 순서를 못박을 것 | §10 신설 |
