# M11 — 진행 상태 접지 (하네스 상태선) 설계

승인일: 2026-07-18. 브레인스토밍으로 확정(기함 선정 → 동반 범위 → 측정 규모 →
성공 기준 → 메커니즘 2회 피벗 — 사용자 결정 6개). 직전 상태: M10 완료(main
`6ad6f08`, 승자 암③ 디코딩 섭동 병합) — 정직 기록에 "uv 1~3/10 불변(다지점
전파 병목 — M11 입력)". 스코핑 입력: ① 소형모델 네이티브 전략 후보 ①~⑤
(메모리, 기함 잠정 ④ grep-first 맵) ② M10 실험 1 리포트의 uv 병목 ③ 외부
참조 기사 — Impossible Research "Schema 하네스"(aitimes 212889): 모델 불변·
하네스 재설계만으로 ARC-AGI-3 공개 세트 42.83%→98.98% 주장(자체 보고·공개
세트 한정 — 수치는 참고, 채용하는 것은 **원리**: ⑴ 상태 접지 — 상태를 모델
기억이 아니라 하네스가 관측 사실로 정의·유지 ⑵ 이해를 실행 가능한 검증에
묶기). loco의 기존 노선("작은 모델은 하네스를 조인다", 기계 검증 게이트)과
동일 축의 외부 근거.

## 1. 배경 — 두 번의 데이터 반전이 설계를 정했다

**출발(브레인스토밍 초안)**: 기함을 "전파 상태 접지"로 선정 — uv(update-vat-
rate)는 세율 변경을 4개 산개 지점에 전파해야 하는 과제로, M10 전 암 공통
1~3/10. 초안 메커니즘은 "편집 성공 시 대체된 옛 텍스트를 프로젝트 전역
재검색해 잔존 지점 알림"(푸터 주입)이었다.

**반전 1 — 픽스처 실물이 푸터를 반증**: uv의 4개 지점은 세율 표기가 전부
다르다 — `DEFAULT_VAT_PERCENT: u32 = 10` / `* 1.10` / `* 110 / 100` /
`* 10 / 100` (산포 함정 설계 의도, 지점별 독립 실패 요구는 M8 스펙 리뷰
산물). 어떤 편집의 옛 텍스트도 다른 지점과 텍스트 매치되지 않아 푸터는
발동 자체가 안 된다. 표기가 달라도 "같은 값"임을 아는 유일한 오라클은
`cargo test`뿐이고(지점별 독립 테스트가 실패를 열거), 그것은 모델이 직접
실행해야 얻는 정보다 — 하네스가 잔존 지점을 대신 알아낼 방법은 없다.

**반전 2 — M10 승자 배치(스탬프 152633Z) uv 10런 정독이 접지 대상을
바꿨다**: 실패는 세 덩어리로 갈린다.

| 유형 | 런 | 양상 |
|---|---|---|
| 조기 finish (스코프 상실) | 0, 5 | 1~4턴 만에 "구조 요약"·"'10' 출현 목록"을 결과물로 finish — 뮤테이션 0회 |
| 탐색 루프 | 1, 2, 7 | 25턴 내내 read/grep만 — 편집 시작을 못 함 |
| 편집기 턴 소진 | 3, 4, 6, 8 | 편집은 하나 "다 했는지" 몰라 새 grep 패턴을 계속 시도, 턴 고갈 |
| 성공 | 9 | grep → 편집 → `cargo test`(exit 101 목격) → 수정 → `cargo test`(exit 0) → finish, 15턴 |

10런 중 진짜 `cargo test`를 실행한 것은 성공 런 9 하나뿐. run-3은
`cargo test 2>&1 | tail -50`으로 실행해 **파이프라인의 exit code가 tail의
0으로 보고**됐다 — 모델도, "exit code: 0"을 파싱하는 finish_nudge 무장
조건도 함께 속는다.

**결론**: 모델이 잃는 상태는 "남은 지점 목록"(하네스가 알 수 없음)이 아니라
**자기 진행 상태**(하네스가 이미 정확히 앎)다 — ① 뭘 고쳤나(성공 뮤테이션
경로) ② 검증했나(뮤테이션 후 run_command 여부·exit code) ③ 턴이 얼마나
남았나. 기함을 **진행 상태 접지(progress-state grounding)**로 개명하고,
하네스가 이 세 사실을 조건부로 주입한다(§4). 전파 완주는 간접 경로로
다룬다: "verification: none …" 접지가 `cargo test` 실행을 유도하고, 실패
테스트가 남은 지점을 열거한다 — 유일한 통과 런 9의 패턴을 유도(affordance)
로 재현하는 구조다. 강요(차단)가 아닌 유도인 것은 M10 암② 풍선효과의 실측
교훈을 따른다.

## 2. 목표·비목표·성공 기준

**목표**:
1. 상태선 주입(STATUS_NOTE) — §4
2. 부속: 파이프 exit code 안내 — §5
3. 0단계 법의학 공식화(측정 0, 전제 반전 시 스펙 회귀) — §3
4. 실험 2 — 사전등록 → 무인 수행 → 판정·병합 — §6
5. 비측정 소품 2건(T7 이월 테스트·exp_metrics 파일 귀속) — §7
6. 문서 갱신 — baselines.md 실험 2 절·README·CLAUDE.md

**비목표 (명시)**:
- **조기 finish 개입(런 0·5형)** — 뮤테이션 0회 finish는 읽기 전용 과제
  (find-definition류)의 정당한 종결과 하네스 수준에서 구분 불가. 어떤
  케이던스 주입도 1~4턴 finish에 못 미친다. 잔여로 명기, 백로그(§9)
- **테스트 실패 요약 주입(오라클 대행)** — 하네스가 `cargo test`를 대신
  실행·요약해 주는 안은 ⑴ 실사용에서 검증 명령을 하네스가 모르고 ⑵ eval
  에서는 check 오라클 유용(리워드 해킹 축)이라 배제. 모델을 오라클로
  안내하는 것(§4)까지만
- grep-first 맵(후보 ④) — M12+ 후보로 백로그 유지(M8 실측: 항해는 grep이
  이미 뚫음 — 근거 상대 열세)
- 문법 제약 디코딩(후보 ②)·F uv1 content 누락 대응 — 별개 축, 백로그
- pipefail 강제 — sh/cmd 크로스 플랫폼 위험, §5의 안내 한 줄로 대체
- config 토글 신설 없음(M5/M9/M10 관례 — 스캐폴딩은 항상-켜짐), 픽스처
  변경 일절 없음

**성공 기준**:
1. 게이트: `cargo test` + `cargo clippy --all-targets -- -D warnings` +
   `eval tasks --verify` 12/12 + `eval tasks-large --verify` 3/3
2. 행동 지표(주 — 수치는 §3 확정 후 사전등록에서 고정): ① 탐색 루프 런
   (성공 뮤테이션 0회 max_turns) 감소 ② 뮤테이션 발생 런의 **검증 실행률**
   상승. ②의 지표 정의(주의): 뮤테이션 후 **cargo 명령**(`cargo test`/
   `cargo check` 류) run_command ≥1회 — 하네스의 VERIFY_NUDGE 의미론(모든
   run_command 인정)과 다르다. grep을 run_command로 돌리는 런(152633Z
   uv run-6 실측)을 "검증"으로 오집계하지 않기 위한 지표 측 강화이며,
   정확한 명령 패턴은 §3 노트 확정 후 사전등록에서 고정. 대조 실측
   (152633Z uv, cargo 기준): ①=3/10(런 1·2·7) ②=3/5(런 3·4·9 / 뮤테이션
   런 3·4·6·8·9). 소표본 규칙: 발생 런 3런 미만이면 전수 나열 + 방향
   판정(M9 §2 승계)
3. 통과율(보조): uv 통과율(대조 1/10) 상승, fm 비악화, tasks/ 스포트
   (ornith@8K, v2 조건) ≥ 33/36 — M10 실측 35/36
4. 신규 모델-대면 텍스트 전부 영문, 사용자 CLI 메시지 한국어

## 3. 0단계 — 법의학 공식화 (구현 착수 전, 측정 비용 0)

브레인스토밍 중 수행한 미니 판독(§1 반전 2)을 공식 노트
`docs/research/2026-07-18-m11-uv-progress-forensics.md`로 확장·문서화한다:

- ⓐ 152633Z uv 10런 분류표(런별: 턴 수·도구 시퀀스·뮤테이션 수·검증 실행
  여부·stop_cause·3덩어리 귀속) — 세션 판독의 재검증 포함
- ⓑ 동 배치 fm 10런 + 32K 검증 배치 20런에 같은 분류를 적용 — 3덩어리가
  과제·컨텍스트 크기를 넘어 일반화되는지 확인(상태선 내용·발동 조건의
  최종 확정 근거)
- ⓒ 픽스처 표기 산포 근거(4지점 diff)와 "재검색 푸터 무산" 판정 기록 —
  후속 마일스톤이 같은 안을 재발명하지 않도록
- ⓓ run-3형 파이프 위장의 전수 확인(전 배치에서 `cargo test`가 파이프로
  실행된 런 수) — §5의 발동 범위 실측

**전제 반전 시 스펙 회귀**: ⓐ·ⓑ의 공식 판독이 §1의 3덩어리 분류를
뒤집으면(예: 탐색 루프 런에 실제 뮤테이션이 있었다면) 스펙을 개정하고
재리뷰한다(M10 §3 관례).

## 4. 개입 — 상태선 주입 (STATUS_NOTE)

하네스가 이미 추적하는 진행 상태를 조건부로 툴 결과에 접지한다. 유도형 —
차단·거부 없음, 모델은 무시할 자유가 있다.

- **채널(핀)**: 기존 교정문과 동일한 `note` 채널(`merge_note` →
  `session.push_tool_result(tool, args, body, note)`) — `body`는 불변이므로
  ⑴ 반복 해시(`tracker.record`가 body만 해시 — mod.rs track_and_note)와
  자동 격리: 동일 반복 호출은 상태선이 있어도 RepetitionStop에 도달한다
  ⑵ 오류 스트릭 키(body 첫 문장)·finish_nudge의 exit 파싱(body 첫 줄)도
  무영향. 병합 순서: 기존 노트(salvage·교정·니지) **뒤**에 마지막으로.
  RepetitionStop 턴(stop=true)에는 주입하지 않는다(기존 우선순위 관례).
- **상태(run() 지역 — tracker·finish_nudge와 동일 수명)**:
  - `mutated_paths: Vec<String>` — 성공 뮤테이션(edit_file/write_file
    디스패치 Ok)의 `path` 인자. 렉시컬 정규화(M10 §4 관례 —
    `Path::components` 정리, 파일시스템 조회 없음), 중복 제거·삽입 순서
    유지. `path` 부재/비문자열이면 수집 생략(BadArgs는 애초에 실패)
  - `last_cmd_exit: Option<String>` — 마지막 run_command Ok의 body 첫 줄
    `exit code: N` (finish_nudge §4-2와 같은 파싱 — 타임아웃·Err 본문에는
    이 줄이 없어 None 유지)
  - `mutated_since_verify`(기존, VERIFY_NUDGE)·`turns`(기존) 재사용
- **본문(영문, 경로 외 코드 유사 텍스트 금지 — M5 "줄번호 접두" 교훈:
  모델이 다음 search에 복사할 재료를 주지 않는다)**:
  - 뮤테이션 0회: `[status] files edited: none yet | turns: {t} of {max} used`
  - 뮤테이션 ≥1·검증 미실행(`mutated_since_verify == true`):
    ```
    [status] files edited: {k} ({p1}, {p2}, ... and {n} more)
             verification: none since your last edit
             turns: {t} of {max} used
    ```
    경로 목록은 5개 캡 + `and {n} more`
  - 뮤테이션 ≥1·검증 실행됨: `verification:` 줄을
    `verification: last command exited {N}`으로 (last_cmd_exit 없으면
    `verification: last command gave no exit code`). 의미론 주의: 이
    줄은 VERIFY_NUDGE와 같은 "모든 run_command 인정" 기준을 상속한다 —
    하네스는 어느 명령이 진짜 검증인지 알 수 없으므로 문구도 "last
    command"라는 사실 서술로 한정(테스트 통과를 주장하지 않는다). §2
    기준 2 ②의 지표는 이보다 강한 cargo 기준(지표 측 강화)
- **발동(턴당 최대 1회 — 아래 어느 하나라도 참이면 주입)**:
  1. 이번 턴이 성공 뮤테이션 디스패치 (목록 즉시 접지 — 편집기 런 겨냥)
  2. `mutated_paths`가 빈 상태로 turns가 5·10·15·20 도달 (탐색 루프 런
     1·2·7 겨냥 — "files edited: none yet"이 착수 압력)
  3. turns 15·20 도달 (무조건 — 소진 런의 페이싱)
  "턴"은 기존 `turns` 카운터 값 — 도달 판정 시점(증가 전/후)은 무액션
  턴(파싱 재시도·length)도 turns를 증가시키는 현행 의미론과 함께 플랜
  에서 핀 고정한다(핵심 계약: 임계 도달 턴에 툴 결과가 있으면 그 턴에
  정확히 1회 주입, 무액션 턴은 주입 없이 통과 가능). 게이트 거부 턴은
  뮤테이션이 아니므로 조건 1 비발동(조건 2·3은 발동 가능 — 거부 결과도
  툴 결과 턴이다).
- **관측성**: `[status]` 마커가 트랜스크립트 note에 남는다 —
  exp_metrics(§6)가 발동 수·발동 후 행동 전환을 유도. REPL Notice는 내지
  않는다(교정이 아닌 상시 접지 — 소음 방지).
- **예산**: 회당 ~30-40토큰, 통상 런 ≤8회(~300토큰/8K), 최악(뮤테이션
  다수)도 턴당 1회 캡으로 ≤25회. note는 tool_result 메시지에 병합되므로
  pack() 축약 대상 — 옛 상태선이 축약돼도 최신 주입이 항상 현재 상태를
  다시 접지한다(소실 무해).

## 5. 부속 — 파이프 exit code 안내

`run_command` 결과 본문(tools/run_command.rs) 조립 시, 명령 문자열에
**따옴표 밖** `|`가 있으면 끝에 한 줄을 덧붙인다:

> `note: this command is a pipeline - the exit code reflects only the last command in the pipe`

- 따옴표 인지 스캐너(single/double quote 상태 추적, ~20줄) — 모델들이
  grep 패턴 내 `\|`를 상용하므로(152633Z 실측) naive `contains('|')`는
  거의 매 grep에 오발한다. 이스케이프 엣지 케이스의 잔여 오발은 허용
  오차(정보 한 줄, 무해).
- body에 들어가므로 반복 해시에 포함되지만 결정론적(같은 명령 → 같은
  노트)이라 반복 탐지 무영향. 첫 줄(`exit code: N`) 불변 —
  finish_nudge·VERIFY_NUDGE 파싱 무영향.
- pipefail 강제는 하지 않는다(§2 비목표): POSIX sh에 pipefail이 없고
  cmd /C와의 대칭도 깨진다. 진실을 강제하는 대신 위장을 **알려준다**.

## 6. 실험 2 — 사전등록 요강

본문은 `docs/experiments/2026-07-18-progress-grounding/pre-registration.md`
로 별도 작성·승인한다(스펙 승인 ≠ 사전등록 승인 — PROTOCOL.md).

- **가설**: H1 "files edited: none yet" 접지는 탐색 루프 런(뮤테이션 0회
  max_turns)을 줄인다. H2 "verification: none since your last edit" 접지는
  뮤테이션 런의 검증 실행률을 높인다.
- **암 = 브랜치(M10 §7-2 관례)**: 대조군은 **신규 측정 없이 152633Z 재사용**
  (당시 코드 = 병합된 암③ = 현 main과 에이전트 행동 동일; effective_config
  대조로 조건 일치 검증). 정직 각주: 비동일 세션·열적 조건의 재사용 한계를
  리포트에 명기. 개입군 = `m11/status-note` 브랜치(main + §4 + §5).
- **표본**: ornith-1.0-9b@8K(로드 12288) × uv+fm × `--repeats 10 --seed 0`
  (대조와 동일 조건) + tasks/ 스포트 36런(v2 조건: timeout 60·로드 8192).
  예산 ≈ 1.7~2h + 게이트·빌드(M10 실측 암당 ~45-50분 근거).
- **32K 검증**: 주지표 충족 시에만 선택 수행(대조 32K 스탬프 존재 — 실험 1
  리포트 참조). 포함 여부를 사전등록에 명시.
- **지표(exp_metrics.py 확장)**: `[status]` 발동 수·첫 뮤테이션 턴·검증
  실행 런 비율(파이프/비파이프 구분)·뮤테이션 0회 max_turns 분류·파이프
  노트 발동 수 + 기존 지표 전부. §7의 파일 귀속도 이번 확장에 포함.
  `--selftest` 갱신.
- **판정 규칙(사전 확정)**: 주 = §2 기준 2(수치는 §3 노트 확정 후
  사전등록에서 고정). 보조 = §2 기준 3 + 평균 턴·오버플로·salvage 빈도
  (컨텍스트 압박 감시). 승자 규칙: 주지표 우세(열세 없음)면 main 병합,
  미충족이면 비병합·분석 문서화(M10 관례 — 패자 브랜치 보존).
- **무인 수행**: loco-experiment-runner. M10 운영 교훈 반영 — 러너 지시에
  "배치 감시는 통지 의존 금지, 종료를 exit code·스탬프 디렉토리로 직접
  확인"을 명시.
- **중단 규칙**: LLM 에러·부분 리포트 시 해당 배치 1회 재수행, 재실패면
  중단·보고. Ctrl+C 부분 리포트 폐기 후 재수행(M10 승계).

## 7. 비측정 소품

- **T7 이월**: 섭동 오버라이드가 finish-재시도 턴에서 유지되는 회귀 테스트
  — M10 §5 원복 핀("finish 시도 턴은 스트릭 불변 → 오버라이드 유지")의
  미커버 분기. 테스트만, 동작 변경 없음.
- **exp_metrics 파일 귀속**: S/R 오류를 대상 파일별로 귀속해 출력 — M10
  §9 기준 3②의 운영화 각주(스펙 §2↔§7-3 미정합) 해소. §6 확장에 포함.

## 8. 테스트

- 상태선(§4): 발동 조건 3종 각각, 턴당 1회 캡(조건 중복 시 1회), 본문
  3상태(0뮤테이션/미검증/검증됨), 경로 5개 캡 + `and N more`, 렉시컬
  정규화 합산(`./src/x.rs` = `src/x.rs`), **note 채널 격리**(상태선 존재
  하에 동일 호출 5반복이 RepetitionStop 도달), 기존 교정문과 병합 순서,
  영문 텍스트
- 파이프 안내(§5): `cargo test | tail -50` 발동, `grep "a\|b" f` 비발동,
  파이프 없는 명령 비발동, 첫 줄 exit code 불변
- 소품(§7): finish-재시도 오버라이드 유지 테스트, exp_metrics 파일 귀속
- `scripts/exp_metrics.py --selftest`: 신규 마커([status]·파이프 노트)·
  파일 귀속 검증 추가 (cargo 게이트 밖 자체 게이트)

## 9. 리스크와 백로그

**리스크**:
- 컨텍스트 압박 — 통상 ~300토큰/런, 최악 턴당 1회 캡. 보조 지표(평균 턴·
  오버플로 발생)로 감시, 압박 신호 시 발동 조건 축소(케이던스 10턴)를
  후속 조정으로
- 복사 오염 — 상태선은 경로만 담는다(경로는 기존 툴 결과에 상존하는
  재료). `[status]` 마커가 search 블록에 복사돼 나타나는지 exp_metrics로
  관측 가능
- 접지 무시 — 소형 모델이 note를 안 읽으면 H1/H2 기각 → 비병합, 그 자체가
  "유도 한계" 데이터(M10 암② 기각과 동급의 정보)
- 조기 검증 유발 — 상태선이 전 지점 수정 전 `cargo test`를 유도할 수 있음
  — 실패 열거는 남은 지점 정보이므로 오히려 목적 부합(무해)
- 대조군 재사용의 비동시성 — effective_config 대조 + 리포트 한계 각주.
  판정이 경계선이면 대조군 재측정을 사전등록 개정으로

**백로그 (M11에서 하지 않음)**:
- 조기 finish 잔여(런 0·5형) — 읽기 전용 정당 종결과의 구분 문제가 미해결.
  후보: 과제 프롬프트의 뮤테이션 요구 판별(비휴리스틱 방법 부재), 데이터
  축적 대기
- 테스트 실패 요약 접지 — 실사용 한정(check 명령 발견 휴리스틱 필요),
  eval 오라클 유용 금지 원칙과 함께 설계해야 함
- grep-first 맵(후보 ④, M12 후보)·문법 제약 디코딩(후보 ②)·소함수 게이트
  (후보 ⑤)·F uv1 write_file content 누락(툴콜 형성 버그)
- 32K 확장 검증(주지표 미충족 시 생략된 경우), 섭동 값 스윕(M10 승계)
- 기존 승계: 오버플로 내성 4건·CARGO_HOME 격리·트랙 B 품질·16K 프로파일·
  F fd1형 엄격 손실·실패 턴 컨텍스트 제거
