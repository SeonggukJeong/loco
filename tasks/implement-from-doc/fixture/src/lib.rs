/// 런랭스 인코딩(RLE).
/// 연속으로 반복되는 문자를 `문자 + 반복횟수`로 축약한다.
/// 반복이 1회인 문자에도 횟수 1을 붙인다.
/// 예: "aaabbc" -> "a3b2c1", "" -> "".
/// 유니코드 문자 단위(char)로 처리한다.
pub fn rle(s: &str) -> String {
    let _ = s;
    todo!()
}
